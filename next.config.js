/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['https://fakestoreapi.com/', 'links.papareact.com', 'www.iconarchive.com', 'fakestoreapi.com']
  }
}

module.exports = nextConfig
